# Render Deployment Configuration

# Node.js version
NODE_VERSION=18

# Build command (Render will run this)
BUILD_COMMAND=npm install

# Start command (Render will run this to serve the site)
START_COMMAND=npm start

# Port configuration (Render automatically sets this)
PORT=10000
